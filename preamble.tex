%Headings should not be larger than 14pt, but I have ignored this.

%Chapters must be numbered in Roman numerals
%I have included the code for this below, but it is commented out.

%Footnote numberings should reset at each chapter
%I didn't implement this, and I'm not sure what the default behaviour is

\UseRawInputEncoding

\pdfminorversion = 7

\usepackage{soul} % Highlight environment with \hl{}

\usepackage[export]{adjustbox}
\usepackage{amssymb} % include additional mathematical symbols
\usepackage{flafter} % Figures must appear after their definitions
\usepackage{graphicx} % include images
\usepackage[hidelinks,
            pdftex,
            pdfauthor={Fill in author},
            pdftitle={Fill in title}]{hyperref} % typeset URLs, autoref
\usepackage{pdfpages}
\usepackage{mathtools}
\usepackage[all]{nowidow} % prevent orphan lines and widow lines
\usepackage[comma,square,super]{natbib}
\usepackage{parskip}  % no paragraph indents
\usepackage[section]{placeins} % Floats appear in section where they are defined
\usepackage{setspace} % support double spacing
\usepackage{subcaption} % caption subfigures

% Include quote for each chapter
\usepackage{epigraph}
\renewcommand{\epigraphsize}{\small}
\renewcommand{\textflush}{flushright}
\renewcommand{\sourceflush}{flushright}
\let\originalepigraph\epigraph 
\renewcommand\epigraph[2]{\originalepigraph{\textit{#1}}{\vspace{1\baselineskip}\textsc{#2}}}
\setlength\epigraphwidth{.45\textwidth}
\setlength\epigraphrule{0pt}
%\setlength\beforeepigraphskip{1\baselineskip}
\setlength\afterepigraphskip{2\baselineskip}

% Citations
\usepackage[english]{babel}  
\renewcommand{\chapterautorefname}{Chapter}
\renewcommand{\sectionautorefname}{Section}
\renewcommand{\subsectionautorefname}{Subsection}

% Set margin sizes
\usepackage{geometry}
\geometry{
a4paper,
left = 1.5in,
top = 1.25in,
right = 1.0in,
bottom = 1.0in,
}

\renewcommand{\arraystretch}{1.2}

% Adjust header formats
%\renewcommand{\thechapter}{\Roman{chapter}} % Use Roman numerals for chapters
%\uppercase\expandafter{\romannumeral \thechapter \relax}
\usepackage[explicit]{titlesec}
\titleformat{\chapter}
{\LARGE\filcenter}{\MakeUppercase{Chapter \thechapter:}}{1em}{\MakeUppercase{#1}} % Original (CHAPTER 4)
\titleformat{\section}
{\Large}{\thesection}{1em}{\MakeUppercase{#1}}
\titleformat{\section}
{\large}{\thesection}{1em}{\MakeUppercase{#1}}
\titlespacing\chapter{0pt}{12pt plus 4pt minus 2pt}{6pt plus 2pt minus 2pt}
\titlespacing\section{0pt}{12pt plus 4pt minus 2pt}{6pt plus 2pt minus 2pt}
\titlespacing\section{0pt}{12pt plus 4pt minus 2pt}{6pt plus 2pt minus 2pt}
\titlespacing\subsection{0pt}{12pt plus 4pt minus 2pt}{6pt plus 2pt minus 2pt}

% Put page numbers at top-right
\usepackage{fancyhdr}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt} % do not include head rule
\fancyhead[R]{\thepage}

\makeatletter
\patchcmd\chapter{\thispagestyle}{\global\c@footnote\z@\thispagestyle}{}{}
\makeatother

% Prevent page breaks within bibliography entries, etc.
% https://tex.stackexchange.com/questions/94699/absolutely-definitely-preventing-page-break
\newenvironment{absolutelynopagebreak}
  {\par\nobreak\vfil\penalty0\vfilneg
   \vtop\bgroup}
  {\par\xdef\tpd{\the\prevdepth}\egroup
   \prevdepth = \tpd}

\newcommand{\chaptercloser}{
	\vfill
	\begin{center}
	\resizebox{3cm}{!}{
	\includegraphics{image.png}
	}
	\end{center}
	\vfill
}
